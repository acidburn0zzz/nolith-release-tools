- Picked into respective `stable` branches. [`Picked into Stable` <%= version.to_minor %> merged merge requests]:
  - [ ] REFERENCE TO MR TO PICK
- [ ] Check [`ce/<%= version.stable_branch %>` CHANGELOG]
- [ ] Check [`ee/<%= version.stable_branch(ee: true) %>` CHANGELOG]
- [ ] Check [`omnibus-gitlab/<%= version.stable_branch %>` CHANGELOG]
- [ ] Check [`omnibus-gitlab/<%= version.stable_branch(ee: true) %>` CHANGELOG]
- [ ] Merge `ce/<%= version.stable_branch %>` into `ee/<%= version.stable_branch(ee: true) %>`
- [ ] Check for any migrations in CE: `git diff <%= version.previous_tag %>..<%= version.stable_branch %> -- db/`
- [ ] Check for any migrations in EE: `git diff <%= version.previous_tag(ee: true) %>..<%= version.stable_branch(ee: true) %> -- db/`
- [ ] Tag and build (this is CI-automatized) the `<%= version.to_patch %>` version using the [`release` task]:

    ```sh
    bundle exec rake "release[<%= version.to_patch %>]"
    ```
- [ ] Check that packages are [built][omnibus-gitlab-builds] and appears under [`gitlab/gitlab-ee`] and [`gitlab/gitlab-ce`] on https://packages.gitlab.com
- [ ] Add patch notice to [<%= regression_issue.title %>](<%= regression_issue.url %>):

    ```
    `<%= version %>` has been tagged, further fixes will go into `<%= version.next_patch %>` as necessary.
    ```
- [ ] Deploy `<%= version %>` to GitLab.com
- [ ] Create the `<%= version %>` version on https://version.gitlab.com
- [ ] Blog Post:
  - BLOG_POST_MR
  - [BLOG_POST_URL]
- [ ] Tweet:

    ```
    GitLab <%= version %> is now available with DESCRIPTION OF THE FIXES / IMPROVEMENTS HERE. More info: BLOG_POST_URL
    ```
- [ ] Add [`ce/<%= version.tag %>` CHANGELOG] items to [`ce/master` CHANGELOG]
- [ ] Add [`ee/<%= version.tag(ee: true) %>` CHANGELOG] items to [`ee/master` CHANGELOG]
- [ ] Add [`omnibus-gitlab/<%= version.tag %>` CHANGELOG] items [`omnibus-gitlab/master` CHANGELOG]
- [ ] Create the next patch issue (if needed) using the [`patch_issue` task]:

    ```sh
    bundle exec rake "patch_issue[<%= version.next_patch %>]"
    ```

---

For references:
- https://gitlab.com/gitlab-org/omnibus-gitlab/commits/<%= version.stable_branch %>
- https://gitlab.com/gitlab-org/omnibus-gitlab/commits/<%= version.stable_branch(ee: true) %>
- https://gitlab.com/gitlab-org/gitlab-ce/commits/<%= version.stable_branch %>
- https://gitlab.com/gitlab-org/gitlab-ee/commits/<%= version.stable_branch(ee: true) %>

[`Picked into Stable` <%= version.to_minor %> merged merge requests]: https://gitlab.com/dashboard/merge_requests?assignee_id=&author_id=&label_name=Pick+into+Stable&milestone_title=<%= version.to_minor %>&scope=all&sort=id_desc&state=merged
[`ce/<%= version.stable_branch %>` CHANGELOG]: https://gitlab.com/gitlab-org/gitlab-ce/blob/<%= version.stable_branch %>/CHANGELOG
[`ee/<%= version.stable_branch(ee: true) %>` CHANGELOG]: https://gitlab.com/gitlab-org/gitlab-ee/blob/<%= version.stable_branch(ee: true) %>/CHANGELOG
[`omnibus-gitlab/<%= version.stable_branch %>` CHANGELOG]: https://gitlab.com/gitlab-org/omnibus-gitlab/blob/<%= version.stable_branch %>/CHANGELOG.md
[`omnibus-gitlab/<%= version.stable_branch(ee: true) %>` CHANGELOG]: https://gitlab.com/gitlab-org/omnibus-gitlab/blob/<%= version.stable_branch(ee: true) %>/CHANGELOG.md
[`ce/<%= version.tag %>` CHANGELOG]: https://gitlab.com/gitlab-org/gitlab-ce/blob/<%= version.tag %>/CHANGELOG
[`ee/<%= version.tag(ee: true) %>` CHANGELOG]: https://gitlab.com/gitlab-org/gitlab-ee/blob/<%= version.tag(ee: true) %>/CHANGELOG
[`ce/master` CHANGELOG]: https://gitlab.com/gitlab-org/gitlab-ce/blob/master/CHANGELOG
[`ee/master` CHANGELOG]: https://gitlab.com/gitlab-org/gitlab-ee/blob/master/CHANGELOG
[`omnibus-gitlab/master` CHANGELOG]: https://gitlab.com/gitlab-org/omnibus-gitlab/blob/master/CHANGELOG.md
[BLOG_POST_URL]: BLOG_POST_URL
[omnibus-gitlab-builds]: https://dev.gitlab.org/gitlab/omnibus-gitlab/builds
[`gitlab/gitlab-ee`]: https://packages.gitlab.com/gitlab/gitlab-ee
[`gitlab/gitlab-ce`]: https://packages.gitlab.com/gitlab/gitlab-ce
[`release` task]: https://gitlab.com/gitlab-org/release-tools/blob/master/doc%2Frake-tasks.md#releaseversion
[`patch_issue` task]: https://gitlab.com/gitlab-org/release-tools/blob/master/doc%2Frake-tasks.md#patch_issueversion
